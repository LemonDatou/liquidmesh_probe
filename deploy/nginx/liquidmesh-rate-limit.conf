# Loaded in the Nginx http context.
limit_req_zone $binary_remote_addr zone=liquidmesh_public_api:10m rate=30r/m;
proxy_cache_path /var/cache/nginx/liquidmesh-public levels=1:2 keys_zone=liquidmesh_public:10m max_size=32m inactive=1h use_temp_path=off;
